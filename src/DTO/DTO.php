<?php

declare(strict_types=1);

namespace App\DTO;

/**
 * Marker interface for Data Transfer Objects (DTOs).
 *
 * This interface serves as a simple marker to identify classes as DTOs
 * within the application. It does not define any methods, but indicates
 * that an implementing class is primarily used for transferring data
 * between different layers or components of the application.
 */
interface DTO
{
    // This is a marker interface, so it intentionally has no methods.
}